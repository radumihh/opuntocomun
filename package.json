{
  "name": "opunto-compress",
  "version": "1.0.0",
  "private": true,
  "description": "Compress project images (-> webp) and videos (-> webm) into the same folders as the originals, minimizing size without visible quality loss.",
  "type": "module",
  "scripts": {
    "compress": "node scripts/compress.mjs"
  },
  "dependencies": {
    "sharp": "^0.35.3"
  }
}
